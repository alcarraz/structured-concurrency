plugins {
    id 'java'
    id 'application'
}

group = 'com.example'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(25)
    }
}

application {
    mainClass = 'com.example.DemoRunner'
}

dependencies {
    testImplementation platform('org.junit:junit-bom:5.10.0')
    testImplementation 'org.junit.jupiter:junit-jupiter'
}

tasks.named('test') {
    useJUnitPlatform()
}

compileJava {
    options.compilerArgs += "--enable-preview"
}

tasks.withType(JavaCompile).configureEach {
    options.compilerArgs += "--enable-preview"
}

tasks.withType(Test).configureEach {
    jvmArgs += "--enable-preview"
}

tasks.withType(JavaExec).configureEach {
    jvmArgs += "--enable-preview"
}

tasks.withType(Javadoc).configureEach {
    options.addBooleanOption('enable-preview', true)
}

// Demo tasks
tasks.register('demoReactive', JavaExec) {
    group = 'demo'
    classpath = sourceSets.main.runtimeClasspath
    mainClass = 'com.example.demos.ReactiveDemo'
    args = ['basic']
}

tasks.register('demoReactiveExceptions', JavaExec) {
    group = 'demo'
    classpath = sourceSets.main.runtimeClasspath
    mainClass = 'com.example.demos.ReactiveDemo'
    args = ['exceptions']
}

tasks.register('demoFixedReactiveFailFast', JavaExec) {
    group = 'demo'
    classpath = sourceSets.main.runtimeClasspath
    mainClass = 'com.example.demos.ReactiveDemo'
    args = ['fail-fast']
}

tasks.register('demoStructuredFailFast', JavaExec) {
    group = 'demo'
    classpath = sourceSets.main.runtimeClasspath
    mainClass = 'com.example.demos.StructuredDemo'
    args = ['fail-fast']
}

tasks.register('demoStructured', JavaExec) {
    group = 'demo'
    classpath = sourceSets.main.runtimeClasspath
    mainClass = 'com.example.demos.StructuredDemo'
    args = ['normal']
}

tasks.register('demoScopedValues', JavaExec) {
    group = 'demo'
    classpath = sourceSets.main.runtimeClasspath
    mainClass = 'com.example.DemoRunner'
    args = ['scoped-values']
}

tasks.register('demoCompare', JavaExec) {
    group = 'demo'
    classpath = sourceSets.main.runtimeClasspath
    mainClass = 'com.example.DemoRunner'
    args = ['compare']
}

tasks.register('demoCompareFailure', JavaExec) {
    group = 'demo'
    classpath = sourceSets.main.runtimeClasspath
    mainClass = 'com.example.DemoRunner'
    args = ['compare-failure']
}
