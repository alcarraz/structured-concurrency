# Disable parallel test execution (Quarkus tests share CDI container)
junit.jupiter.execution.parallel.enabled = true

# Timeout for individual tests (10 seconds should be plenty)
junit.jupiter.execution.timeout.default = 10s
